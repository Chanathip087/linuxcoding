#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int N, K; cin >> N >> K;
    vector<int> C(N);
    for (int i = 0; i < N; i++) cin >> C[i];
    
    
}